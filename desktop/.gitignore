# Build output - regenerated by build.md's steps, not checked in.
dist/
build/
release/
electron/node_modules/

# The bundled tool binaries are ~200MB and downloaded, not authored - see
# build-resources/tools/README.md for exactly how to re-fetch them.
build-resources/tools/*.exe
